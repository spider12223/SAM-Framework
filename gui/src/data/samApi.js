// Auto-generated from sam_lua_runtime.cpp / sam_js_runtime.cpp — the SAM scripting API surface.
// Powers the in-tool API reference, event catalog, and script-editor autocomplete.
//
// The API is registered identically in the Lua and QuickJS (JS/TS) runtimes. In the shipped
// (engine) build every function below exists in all three languages. Argument order/types and
// return values were read from the C implementations (lua_sam_* / js_sam_*), not guessed.
//
// hostOnly === true means the call mutates or reads authoritative game state and is refused on
// a multiplayer CLIENT (or is otherwise host/singleplayer-only per the framework docs); a
// refused call is a logged no-op, never a crash.
//
// Shared constant sets referenced by `values` below:
//   PLAYER_STATS  — sam_get_stat / sam_set_stat
//   MONSTER_STATS — sam_get_monster_stat / sam_set_monster_stat (no GOLD/EXP; DEX aliases SPEED)
//   EFFECTS       — sam_apply_effect / sam_remove_effect / sam_has_effect / *_monster_effect
//   EQUIP_SLOTS   — sam_get_equipped_item

export const PLAYER_STATS = ["STR", "DEX", "CON", "INT", "PER", "CHR", "HP", "MAXHP", "MP", "MAXMP", "GOLD", "HUNGER", "LEVEL", "LVL", "EXP"];
export const MONSTER_STATS = ["STR", "DEX", "SPEED", "CON", "INT", "PER", "CHR", "HP", "MAXHP", "MP", "MAXMP", "LEVEL", "LVL"];
export const EFFECTS = ["ASLEEP", "POISONED", "STUNNED", "CONFUSED", "DRUNK", "INVISIBLE", "BLIND", "GREASY", "MESSY", "FAST", "PARALYZED", "LEVITATING", "TELEPATH", "VOMITING", "BLEEDING", "SLOW", "MAGICRESIST", "MAGICREFLECT", "VAMPIRICAURA", "SHRINE_RED_BUFF", "SHRINE_GREEN_BUFF", "SHRINE_BLUE_BUFF", "HP_REGEN", "MP_REGEN", "PACIFY", "POLYMORPH", "KNOCKBACK", "WITHDRAWAL", "POTION_STR", "SHAPESHIFT", "WEBBED", "FEAR", "MAGICAMPLIFY", "DISORIENTED", "SHADOW_TAGGED", "TROLLS_BLOOD", "FLUTTER", "DASH", "DISTRACTED_COOLDOWN", "MIMIC_LOCKED", "ROOTED", "NAUSEA_PROTECTION", "CON_BONUS", "PWR", "AGILITY", "RALLY", "MARIGOLD", "ENSEMBLE_FLUTE", "ENSEMBLE_LYRE", "ENSEMBLE_DRUM", "ENSEMBLE_LUTE", "ENSEMBLE_HORN", "LIFT", "GUARD_SPIRIT", "GUARD_BODY", "DIVINE_GUARD", "NIMBLENESS", "GREATER_MIGHT", "COUNSEL", "STURDINESS", "BLESS_FOOD", "PINPOINT", "PENANCE", "SACRED_PATH", "DETECT_ENEMY", "BLOOD_WARD", "TRUE_BLOOD", "DIVINE_ZEAL", "MAXIMISE", "MINIMISE", "WEAKNESS", "INCOHERENCE", "OVERCHARGE", "ENVENOM_WEAPON", "MAGIC_GREASE", "COMMAND", "MIMIC_VOID", "CURSE_FLESH", "NUMBING_BOLT", "DELAY_PAIN", "SEEK_CREATURE", "TABOO", "COURAGE", "COWARDICE", "SPORES", "ABUNDANCE", "GREATER_ABUNDANCE", "PRESERVE", "MIST_FORM", "FORCE_SHIELD", "LIGHTEN_LOAD", "ATTRACT_ITEMS", "RETURN_ITEM", "DEMESNE_DOOR", "REFLECTOR_SHIELD", "DIZZY", "SPIN", "CRITICAL_SPELL", "MAGIC_WELL", "STATIC", "ABSORB_MAGIC", "FLAME_CLOAK", "DUSTED", "NOISE_VISIBILITY", "RATION_SPICY", "RATION_SOUR", "RATION_BITTER", "RATION_HEARTY", "RATION_HERBAL", "RATION_SWEET", "GROWTH", "THORNS", "BLADEVINES", "BASTION_MUSHROOM", "BASTION_ROOTS", "FOCI_LIGHT_PEACE", "FOCI_LIGHT_JUSTICE", "FOCI_LIGHT_PROVIDENCE", "FOCI_LIGHT_PURITY", "FOCI_LIGHT_SANCTUARY", "STASIS", "HP_MP_REGEN", "DISRUPTED", "FROST", "MAGICIANS_ARMOR", "PROJECT_SPIRIT", "DEFY_FLESH", "PINPOINT_DAMAGE", "SALAMANDER_HEART", "DIVINE_FIRE", "HEALING_WORD", "HOLY_FIRE", "SIGIL", "SANCTUARY", "DUCKED"];
// ^ every named effect the engine defines (stat.hpp EFF_*). sam_apply_effect /
// sam_has_effect / sam_remove_effect accept any of these, case-insensitively, plus a
// custom slot as "135".."159" or "CUSTOM:135". Some are internal engine bookkeeping
// and won't do anything sensible if you apply them by hand — read/remove is always safe.
export const EQUIP_SLOTS = ["WEAPON", "SHIELD", "HELMET", "ARMOR", "BREASTPLATE", "GLOVES", "BOOTS", "SHOES", "RING", "AMULET", "CLOAK", "MASK"];

/**
 * Spells a PLAYER can actually cast — the engine's spell_t globals.
 * Barony's items.json lists ~227 spell_* names, but most are monster-only and would
 * silently do nothing from a script, so this is the subset that really works.
 * Used by sam_cast_spell / sam_grant_spell and a class's starting_spells.
 */
// Player-facing spell set for the picker / class starting_spells / sam_cast_spell. The engine
// resolves ANY spell internalName (sam_classes.cpp spellIdFromName over ItemTooltips.spellItems),
// so all of Barony's 223 are technically valid, but most are monster/spawner/foci internals. This
// is the ~142 that have a spellbook, a compendium entry, or a magic staff (i.e. real castable
// player spells), verified against the game's items.json. The picker is searchable.
export const SPELLS = ["SPELL_FORCEBOLT", "SPELL_MAGICMISSILE", "SPELL_FIREBALL", "SPELL_LIGHTNING", "SPELL_COLD", "SPELL_BLEED", "SPELL_POISON", "SPELL_STONEBLOOD", "SPELL_STRIKE", "SPELL_GHOST_BOLT", "SPELL_HEALING", "SPELL_EXTRAHEALING", "SPELL_CUREAILMENT", "SPELL_REMOVECURSE", "SPELL_SLEEP", "SPELL_CONFUSE", "SPELL_SLOW", "SPELL_SPEED", "SPELL_FEAR", "SPELL_WEAKNESS", "SPELL_CHARM", "SPELL_DOMINATE", "SPELL_INVISIBILITY", "SPELL_LEVITATION", "SPELL_FLUTTER", "SPELL_DASH", "SPELL_TELEPORTATION", "SPELL_DIG", "SPELL_OPENING", "SPELL_LOCKING", "SPELL_IDENTIFY", "SPELL_MAGICMAPPING", "SPELL_LIGHT", "SPELL_SALVAGE", "SPELL_SUMMON", "SPELL_SLIME_ACID", "SPELL_SLIME_FIRE", "SPELL_SLIME_METAL", "SPELL_SLIME_TAR", "SPELL_SLIME_WATER",
  "SPELL_ABSORB_MAGIC", "SPELL_ABUNDANCE", "SPELL_ACID_SPRAY", "SPELL_ALTER_ARROW", "SPELL_ALTER_INSTRUMENT", "SPELL_AMPLIFY_MAGIC", "SPELL_ATTRACT_ITEMS", "SPELL_BLESS_FOOD", "SPELL_BLOOD_WARD", "SPELL_BLOOD_WAVES", "SPELL_BOOBY_TRAP", "SPELL_BREATHE_FIRE", "SPELL_CLEANSE_FOOD", "SPELL_COMMAND", "SPELL_COWARDICE", "SPELL_CRAB_FORM", "SPELL_CRAB_WEB", "SPELL_CURSE_FLESH", "SPELL_DEEP_SHADE", "SPELL_DEFACE", "SPELL_DEFY_FLESH", "SPELL_DEMON_ILLU", "SPELL_DETECT_ENEMY", "SPELL_DETECT_FOOD", "SPELL_DISARM", "SPELL_DISRUPT_EARTH", "SPELL_DIVINE_GUARD", "SPELL_DIVINE_ZEAL", "SPELL_DONATION", "SPELL_DRAIN_SOUL", "SPELL_EARTH_ELEMENTAL", "SPELL_ENVENOM_WEAPON", "SPELL_FIRE_SPRITE", "SPELL_FIRE_WALL", "SPELL_FLAME_CLOAK", "SPELL_FORCE_SHIELD", "SPELL_FORGE_JEWEL", "SPELL_FORGE_KEY", "SPELL_GREASE_SPRAY", "SPELL_GUARD_BODY", "SPELL_GUARD_SPIRIT", "SPELL_HEAL_MINOR", "SPELL_HEAL_OTHER", "SPELL_HOLY_BEAM", "SPELL_ICE_WAVE", "SPELL_IGNITE", "SPELL_IMP_FORM", "SPELL_INCOHERENCE", "SPELL_KINETIC_FIELD", "SPELL_LEAD_BOLT", "SPELL_LIGHTNING_BOLT", "SPELL_MAGICIANS_ARMOR", "SPELL_MAXIMISE", "SPELL_METALLURGY", "SPELL_METEOR", "SPELL_MINIMISE", "SPELL_MIST_FORM", "SPELL_MUSHROOM", "SPELL_NULL_AREA", "SPELL_NUMBING_BOLT", "SPELL_OVERCHARGE", "SPELL_PINPOINT", "SPELL_PRESERVE", "SPELL_PROF_COUNSEL", "SPELL_PROF_GREATER_MIGHT", "SPELL_PROF_NIMBLENESS", "SPELL_PROF_STURDINESS", "SPELL_PSYCHIC_SPEAR", "SPELL_RAT_FORM", "SPELL_REFLECT_MAGIC", "SPELL_RESHAPE_WEAPON", "SPELL_REVERT_FORM", "SPELL_SABOTAGE", "SPELL_SANCTUARY", "SPELL_SCEPTER_BLAST", "SPELL_SCRY_ALLIES", "SPELL_SCRY_TRAPS", "SPELL_SCRY_TREASURES", "SPELL_SEEK_ALLY", "SPELL_SELF_POLYMORPH", "SPELL_SHADOW_TAG", "SPELL_SHATTER_OBJECTS", "SPELL_SHRUB", "SPELL_SIGIL", "SPELL_SLAM", "SPELL_SPIDER_FORM", "SPELL_SPIN", "SPELL_SPIRIT_WEAPON", "SPELL_SPLINTER_GEAR", "SPELL_SPORES", "SPELL_SPRAY_WEB", "SPELL_STEAL_WEAPON", "SPELL_TELEKINESIS", "SPELL_TELEPULL", "SPELL_THORNS", "SPELL_TROLLS_BLOOD", "SPELL_TROLL_FORM", "SPELL_TUNNEL", "SPELL_TURN_UNDEAD", "SPELL_VAMPIRIC_AURA", "SPELL_VOID_CHEST", "SPELL_WINDGATE"];

/**
 * Barony's own bound action names, as a script sees them. Reacting to one of these
 * follows whatever the player rebound it to — that's why you use these instead of a
 * raw key, and why a mod can't collide with someone's keybinds.
 * Defaults for reference: Attack=Mouse1, Use=Mouse3, Defend=Space, Cast Spell=F,
 * "Hotbar Up / Select"=Mouse2 (right-click). Names are space/case-sensitive.
 */
export const ACTIONS = [
  "Attack", "Defend", "Use", "Cast Spell", "Sneak",
  "Hotbar Up / Select", "Hotbar Down / Cancel", "Hotbar Left", "Hotbar Right",
  "Call Out", "Command NPC", "Quick Turn",
];

export const SAM_FUNCTIONS = [
  // ---- Logging ----------------------------------------------------------------
  { name: "sam_log", category: "Logging", hostOnly: false,
    params: [{ name: "msg", type: "string" }],
    returns: "nothing", desc: "Write a line to sam_log.txt (the only output channel). Also exposed as sam.log(msg) in Lua." },
  { name: "sam_message", category: "Logging", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "text", type: "string" }],
    returns: "true on success (boolean)", desc: "Show a line in a player's in-game message log." },

  // ---- Player state -----------------------------------------------------------
  { name: "sam_get_stat", category: "Player state", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "stat", type: "string", values: PLAYER_STATS }],
    returns: "the stat value (number; 0 on client/invalid)", desc: "Read a live player stat. Refused on a multiplayer client." },
  { name: "sam_set_stat", category: "Player state", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "stat", type: "string", values: PLAYER_STATS }, { name: "value", type: "int" }],
    returns: "true on success (boolean)", desc: "Set a live player stat, bounded (HP never exceeds MAXHP, stats clamped, etc.). Syncs the change to the owning client." },
  { name: "sam_set_move_speed", category: "Player state", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "mult", type: "number" }],
    returns: "true on success (boolean)", desc: "Set a player's move-speed multiplier, clamped to [0.1, 3.0]. Host-only; syncs to the owning client. 1.0 is normal speed." },
  { name: "sam_add_move_speed", category: "Player state", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "delta", type: "number" }],
    returns: "the new multiplier (number)", desc: "Add to a player's move-speed multiplier (the result is clamped to [0.1, 3.0]). Additive counterpart to sam_set_move_speed — use it to stack a bonus onto whatever the multiplier already is (e.g. +0.1 on top of a 2.0 from another ability). Host-only; syncs to the owning client." },
  { name: "sam_get_move_speed", category: "Player state", hostOnly: false,
    params: [{ name: "player", type: "int" }],
    returns: "the multiplier (number; 1.0 if unset/invalid)", desc: "Read a player's move-speed multiplier. Readable on clients." },
  { name: "sam_level_up", category: "Player state", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "count", type: "int", optional: true }],
    returns: "true on success (boolean)", desc: "Level a player up count times (default 1) through the real engine path: attribute rolls, HP/MP gain, the level-up screen and sound, and full client sync — the actual benefits, unlike bumping LVL with sam_set_stat. Host-only. Fires the player.on_level_up hook once per level." },
  { name: "sam_get_floor", category: "Player state", hostOnly: false,
    params: [],
    returns: "the current dungeon level (number, 0-based)", desc: "Get the current floor/dungeon level." },
  { name: "sam_get_class", category: "Player state", hostOnly: false,
    params: [{ name: "player", type: "int" }],
    returns: "the class name (string; nil/null if invalid)", desc: "Get a player's class name (vanilla or custom)." },
  { name: "sam_get_race", category: "Player state", hostOnly: false,
    params: [{ name: "player", type: "int" }],
    returns: "the race id (string; nil/null if invalid)",
    desc: "Get a player's race: a custom race's \"namespace:race\" id, or the vanilla race name (\"human\", \"skeleton\", …). Use it in a race behavior script to gate logic to players of that race." },
  { name: "sam_get_kills", category: "Player state", hostOnly: false,
    params: [{ name: "player", type: "int" }],
    returns: "kills this session (number)", desc: "Get the SAM-tracked per-player kill count for this session." },
  { name: "sam_get_time_played", category: "Player state", hostOnly: false,
    params: [],
    returns: "ticks since the run started (number, 50/sec)", desc: "Get elapsed game ticks for the current run." },
  { name: "sam_play_sound", category: "Player state", hostOnly: true,
    params: [{ name: "sound_id", type: "int|string" }, { name: "vol", type: "int", optional: true }],
    returns: "true on success (boolean)", desc: "Play a sound for all connected players. sound_id is a vanilla numeric index OR the \"namespace:sound\" id of a custom sound bundled in the mod. vol 0-255 (default 128)." },

  // ---- Rewards ----------------------------------------------------------------
  { name: "sam_grant_item", category: "Rewards", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "item_name", type: "string" }],
    returns: "true on success (boolean)", desc: "Give a vanilla item (e.g. \"IRON_DAGGER\") to a player. Local player only for now." },
  { name: "sam_grant_gold", category: "Rewards", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "amount", type: "int" }],
    returns: "true on success (boolean)", desc: "Add gold to a player (clamped to >= 0), syncing the client HUD." },
  { name: "sam_spawn_item", category: "Rewards", hostOnly: true,
    params: [{ name: "x", type: "int" }, { name: "y", type: "int" }, { name: "item_name", type: "string" }],
    returns: "true on success (boolean)", desc: "Spawn a ground item (vanilla name) at map tile (x, y)." },
  { name: "sam_item_id", category: "Rewards", hostOnly: false,
    params: [{ name: "name", type: "string", values: ["vanilla ITEM name", "\"namespace:item\" (custom)"] }],
    returns: "the item's numeric type id (int), or nil/null if unknown",
    desc: "Resolve an item's numeric type id — compare it against event fields like on_block's shield_type to react only to a specific item. Accepts a vanilla name or a custom \"namespace:item\"." },
  { name: "sam_get_item_category", category: "Rewards", hostOnly: false,
    params: [{ name: "item", type: "any", values: ["numeric item id (e.g. an event's item_type)", "vanilla name", "\"namespace:item\""] }],
    returns: "the category name (string) e.g. \"GEM\", or nil/undefined if unknown",
    desc: "The category of an item (WEAPON / ARMOR / GEM / POTION / SCROLL / SPELLBOOK / …). Pass an event's item_type to react by category — e.g. reward the player for identifying any GEM." },

  // -------- World ops: position / teleport / spawn / inventory --------
  { name: "sam_get_player_uid", category: "World", hostOnly: false,
    params: [{ name: "player", type: "int" }],
    returns: "the player's entity uid (int), or nil/null if not in-game",
    desc: "Get a player's entity uid, so the uid-based world-ops (get/set position) can act on that player's body." },
  { name: "sam_get_position", category: "World", hostOnly: false,
    params: [{ name: "uid", type: "int" }],
    returns: "tile x, tile y (two values in Lua; an [x, y] array in JS), or nil/null if the uid is gone",
    desc: "Read any entity's map-tile position (player, monster or ground item). Get a player's uid with sam_get_player_uid." },
  { name: "sam_set_position", category: "World", hostOnly: true,
    params: [{ name: "uid", type: "int" }, { name: "tile_x", type: "int" }, { name: "tile_y", type: "int" }],
    returns: "true on success (boolean); false if refused (out of bounds, or a player teleport blocked by a wall)",
    desc: "Move an entity to a map tile. Players go through the safe teleport path (can't tunnel into walls); other entities are relocated and re-broadcast to clients." },
  { name: "sam_spawn_monster", category: "World", hostOnly: true,
    params: [{ name: "tile_x", type: "int" }, { name: "tile_y", type: "int" }, { name: "monster_name", type: "string", values: ["vanilla monster name, e.g. \"skeleton\", \"shopkeeper\""] }, { name: "shop_type", type: "int", optional: true }],
    returns: "the new monster's uid (int), or nil/null if the name is unknown or the tile is blocked",
    desc: "Summon a monster at a map tile. \"shopkeeper\" makes a working shop; the optional shop_type (0-14) picks the store kind." },
  { name: "sam_spawn_portal", category: "World", hostOnly: true,
    params: [{ name: "tile_x", type: "int" }, { name: "tile_y", type: "int" }],
    returns: "the new portal's entity uid (int), or nil/null if the tile is out of bounds",
    desc: "Spawn a purely-DECORATIVE portal (the swirling vortex) at a map tile — it animates and glows but is never interactive and never sends anyone to the next floor. Walkable, so a player can stand on it. Returns the uid so you can move it (sam_set_position) or clear it (sam_remove_entity) — e.g. a portal-gun marker. Host-only. Multiplayer: the portal is host-authoritative and NOT network-synced, so only the host renders it — connected clients won't see it (your teleport/logic still runs host-side)." },
  { name: "sam_remove_entity", category: "World", hostOnly: true,
    params: [{ name: "uid", type: "int" }],
    returns: "true on success (boolean); false for an unknown uid or a player",
    desc: "Remove a non-player world entity by uid — a sam_spawn_portal marker, a spawned monster, a companion, a ground item, etc. Refuses players (use the normal death/teleport paths for those). Frees any light the entity owned." },
  { name: "sam_spawn_companion", category: "World", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "model_id", type: "string", values: ["a registered custom model id, e.g. \"mymod:star_platinum\""] }, { name: "scale", type: "number", optional: true }],
    returns: "the new companion's entity uid (int), or nil/null (bad player / unregistered model / off-host)",
    desc: "Spawn a floating COMPANION (a JoJo-style \"Stand\" / familiar) that renders one of your custom .vox models and trails the player a short distance behind, with a gentle hover. Follows the player every frame and faces where they face. Optional scale (default 1.0, capped at 8) sizes the model. Drive the punch motion with sam_companion_punch, and clear it with sam_remove_entity. It's a decorative follower (PASSABLE, no AI, does no damage on its own — pair it with sam_cast_spell / sam_deal_damage for the actual attack). Host-only; not network-synced (host renders it). Re-spawn it on each new floor (entities are cleared on descent)." },
  { name: "sam_companion_punch", category: "World", hostOnly: true,
    params: [{ name: "uid", type: "int" }],
    returns: "true if uid is a live companion (boolean); false otherwise",
    desc: "Make a companion THRUST forward for a few ticks — the punch motion. Call it repeatedly on a fast repeating timer (e.g. every 3 ticks) during an ability to read as a continuous ORA-ORA flurry. Purely visual on the companion itself; combine with sam_cast_spell (forward projectile + real damage) and/or sam_get_nearby_entities + sam_deal_damage for the hits." },
  { name: "sam_get_inventory", category: "World", hostOnly: false,
    params: [{ name: "player", type: "int" }],
    returns: "a list of items, each { uid, type, name, count, beatitude, status, identified, equipped }",
    desc: "List a player's inventory. Use each item's uid with sam_remove_item. Empty list for an invalid player." },
  { name: "sam_remove_item", category: "World", hostOnly: true,
    params: [{ name: "item_uid", type: "int" }],
    returns: "true on success (boolean); false if the item is missing or currently equipped",
    desc: "Remove a whole item stack from a player's inventory by its uid (from sam_get_inventory). Refuses an equipped item — unequip it first." },

  // ---- Status effects ---------------------------------------------------------
  { name: "sam_apply_effect", category: "Status effects", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "effect", type: "string", values: EFFECTS }, { name: "ticks", type: "int" }, { name: "strength", type: "int", optional: true }],
    returns: "true unless immune/refused (boolean)", desc: "Apply a status effect to a player for N ticks (50 ticks = 1s). Optional strength sets the tier/magnitude for effects that carry one (e.g. GROWTH stacks) — omit it for the plain default. Targets the player, never a monster." },
  { name: "sam_remove_effect", category: "Status effects", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "effect", type: "string", values: EFFECTS }],
    returns: "true on success (boolean)", desc: "Clear a status effect from a player." },
  { name: "sam_has_effect", category: "Status effects", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "effect", type: "string", values: EFFECTS }],
    returns: "whether the effect is active (boolean)", desc: "Check whether a player currently has a status effect." },
  { name: "sam_get_effect_duration", category: "Status effects", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "effect", type: "string", values: EFFECTS }],
    returns: "remaining ticks (int; 0 if inactive, -1 if permanent)", desc: "How many ticks of an effect are left (50 = 1s) — so a debuff can scale or decay by time remaining. Readable on clients." },
  { name: "sam_get_effect_strength", category: "Status effects", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "effect", type: "string", values: EFFECTS }],
    returns: "strength/tier (int; 0 if inactive)", desc: "The effect's strength/magnitude for effects that store one (GROWTH tiers, potion STR). Readable on clients." },
  { name: "sam_get_effects", category: "Status effects", hostOnly: false,
    params: [{ name: "player", type: "int" }],
    returns: "array/table of { name, ticks, strength }", desc: "Every active effect on a player at once — react to \"any debuff\" or strip all buffs without polling each effect by name. Custom pseudo-effect slots appear as \"CUSTOM:<id>\"." },
  { name: "sam_clear_effects", category: "Status effects", hostOnly: true,
    params: [{ name: "player", type: "int" }],
    returns: "how many effects were cleared (int)", desc: "Strip EVERY active status effect from a player at once — buffs and debuffs, vanilla and custom." },
  { name: "sam_set_effect_duration", category: "Status effects", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "effect", type: "string", values: EFFECTS }, { name: "ticks", type: "int" }],
    returns: "true if the effect was active and retimed (boolean)", desc: "Retime an ALREADY-ACTIVE effect in place, without re-triggering it. No-op if the effect isn't active (never spawns a fresh one). 50 ticks = 1s." },
  { name: "sam_set_effect_strength", category: "Status effects", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "effect", type: "string", values: EFFECTS }, { name: "strength", type: "int" }],
    returns: "true if the effect was active and changed (boolean)", desc: "Change the magnitude/tier of an ALREADY-ACTIVE effect while keeping its remaining duration. strength 1-255." },

  // ---- Inventory --------------------------------------------------------------
  { name: "sam_get_equipped_item", category: "Inventory", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "slot", type: "string", values: EQUIP_SLOTS }],
    returns: "the item name (string; nil/null if slot empty)", desc: "Get the item NAME equipped in a slot (ARMOR==BREASTPLATE, BOOTS==SHOES). Vanilla items only — it can't name a custom item, so use sam_get_equipped_item_id to test for one." },
  { name: "sam_get_equipped_item_id", category: "Inventory", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "slot", type: "string", values: EQUIP_SLOTS }],
    returns: "the numeric item id (int; nil/null if slot empty)",
    desc: "Get the item ID equipped in a slot. Compare it against sam_item_id(\"namespace:item\") to check whether YOUR custom item is equipped — the id is a number, so the name-returning version above can never match it." },
  { name: "sam_is_defending", category: "Player state", hostOnly: false,
    params: [{ name: "player", type: "int" }],
    returns: "whether the player is blocking (boolean)",
    desc: "Whether the player is actually blocking right now — the real engine state, not just the Defend button being down. Works for remote players in multiplayer." },
  { name: "sam_get_inventory_count", category: "Inventory", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "item_name", type: "string" }],
    returns: "total count held (number)", desc: "Count how many of an item (vanilla or custom name) a player holds." },

  // ---- Entities ---------------------------------------------------------------
  { name: "sam_get_nearby_entities", category: "Entities", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "radius", type: "number" }],
    returns: "an array/table of creature UIDs (max 32)", desc: "List UIDs of monsters/players within `radius` tiles of a player (never raw pointers)." },
  { name: "sam_get_facing", category: "Entities", hostOnly: false,
    params: [{ name: "player", type: "int" }],
    returns: "the player's facing yaw in radians in [0, 2π) (number), or nil/null for an absent player",
    desc: "Read which way a player is looking. 0 = +x (east), increasing toward +y — so the forward unit vector is (cos yaw, sin yaw) and 'behind' is yaw + π. Use it to place things relative to a player's facing (a marker in front, a follower behind) or to aim. Host-authoritative for remote players; a client always sees its own facing correctly." },

  // ---- Damage -----------------------------------------------------------------
  { name: "sam_modify_damage", category: "Damage", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "new_value", type: "int" }],
    returns: "nothing", desc: "Rewrite incoming damage (clamped to >= 0). ONLY valid inside an on_before_damage callback." },
  { name: "sam_deal_damage", category: "Damage", hostOnly: true,
    params: [{ name: "entity_uid", type: "uid" }, { name: "amount", type: "int" }],
    returns: "true on success (boolean)", desc: "Deal `amount` damage to any entity by UID (positive = damage); existence-validated." },

  // ---- Monsters ---------------------------------------------------------------
  { name: "sam_monster_path_to", category: "Monsters", hostOnly: true,
    params: [{ name: "uid", type: "uid" }, { name: "tileX", type: "int" }, { name: "tileY", type: "int" }],
    returns: "true if a path was found (boolean)",
    desc: "Path a monster to a tile using the engine's real pathfinder, then put it in the hunt state so it walks there. Tile coordinates, matching sam_get_position. Returns false when the destination is unreachable.",
    gotcha: "Barony has no per-species AI — every creature runs one shared state machine — so this steers that machine rather than replacing a brain." },
  { name: "sam_monster_face", category: "Monsters", hostOnly: true,
    params: [{ name: "uid", type: "uid" }, { name: "tileX", type: "int" }, { name: "tileY", type: "int" }],
    returns: "true on success (boolean)",
    desc: "Turn a monster to look at a tile. Aims at the tile centre. Pair it with sam_monster_charge to aim a charge." },
  { name: "sam_monster_attack", category: "Monsters", hostOnly: true,
    params: [{ name: "uid", type: "uid" }],
    returns: "true on success (boolean)",
    desc: "Make a monster swing immediately, using whatever attack pose its current weapon calls for." },
  { name: "sam_monster_charge", category: "Monsters", hostOnly: true,
    params: [{ name: "uid", type: "uid" }, { name: "ticks", type: "int", optional: true }],
    returns: "true on success (boolean)",
    desc: "Send a monster into a straight-line charge for N ticks (50 = 1 second, default 50, max 500). Aims at its target if it has line of sight, otherwise charges along its current facing.",
    gotcha: "Self-terminating: it stops when the timer ends OR the instant it hits anything, so you cannot wedge a monster in a wall. Drives a charge behaviour that shipped in the engine fully written but unreachable." },
  { name: "sam_is_host", category: "Multiplayer", hostOnly: false,
    params: [],
    returns: "true on the host or in singleplayer (boolean)",
    desc: "Whether this machine is the host. Most functions are host-only and warn on a client; check this first instead of letting a client fill the log with refusals." },
  { name: "sam_player_count", category: "Multiplayer", hostOnly: false,
    params: [],
    returns: "connected players (int)",
    desc: "How many players are actually connected right now." },
  { name: "sam_local_player", category: "Multiplayer", hostOnly: false,
    params: [],
    returns: "player index (int)",
    desc: "The player index THIS machine controls.",
    gotcha: "Not always 0. On a multiplayer client it is that client's own index, which is the assumption most singleplayer-tested mods quietly bake in." },
  { name: "sam_get_monster_stat", category: "Monsters", hostOnly: false,
    params: [{ name: "uid", type: "uid" }, { name: "stat", type: "string", values: MONSTER_STATS }],
    returns: "the stat value (number; 0 if not a monster)", desc: "Read a monster's stat by UID. DEX aliases SPEED." },
  { name: "sam_set_monster_stat", category: "Monsters", hostOnly: true,
    params: [{ name: "uid", type: "uid" }, { name: "stat", type: "string", values: MONSTER_STATS }, { name: "value", type: "int" }],
    returns: "true on success (boolean)", desc: "Set a monster's stat by UID (bounded)." },
  { name: "sam_monster_has_effect", category: "Monsters", hostOnly: false,
    params: [{ name: "uid", type: "uid" }, { name: "effect", type: "string", values: EFFECTS }],
    returns: "whether the monster has the effect (boolean)", desc: "The monster counterpart of sam_has_effect — e.g. react when a monster you just hit is POISONED. Pass a monster UID (from a monster event or sam_get_nearby_entities)." },
  { name: "sam_apply_monster_effect", category: "Monsters", hostOnly: true,
    params: [{ name: "uid", type: "uid" }, { name: "effect", type: "string", values: EFFECTS }, { name: "ticks", type: "int" }],
    returns: "true unless immune (boolean)", desc: "Apply a status effect to a monster by UID for N ticks." },
  { name: "sam_remove_monster_effect", category: "Monsters", hostOnly: true,
    params: [{ name: "uid", type: "uid" }, { name: "effect", type: "string", values: EFFECTS }],
    returns: "true on success (boolean)", desc: "Clear a status effect from a monster by UID — the monster counterpart of sam_remove_effect." },
  { name: "sam_get_monster_effect_duration", category: "Monsters", hostOnly: false,
    params: [{ name: "uid", type: "uid" }, { name: "effect", type: "string", values: EFFECTS }],
    returns: "remaining ticks (int; 0 inactive, -1 permanent)", desc: "How many ticks of an effect a monster has left." },
  { name: "sam_get_monster_effect_strength", category: "Monsters", hostOnly: false,
    params: [{ name: "uid", type: "uid" }, { name: "effect", type: "string", values: EFFECTS }],
    returns: "strength/tier (int; 0 if inactive)", desc: "A monster effect's strength/magnitude." },
  { name: "sam_get_monster_effects", category: "Monsters", hostOnly: false,
    params: [{ name: "uid", type: "uid" }],
    returns: "array/table of { name, ticks, strength }", desc: "Every active effect on a monster at once (custom slots appear as \"CUSTOM:<id>\")." },
  { name: "sam_kill_monster", category: "Monsters", hostOnly: true,
    params: [{ name: "uid", type: "uid" }],
    returns: "true on success (boolean)", desc: "Kill a monster by UID (runs its normal death + drops; fires on_monster_died)." },
  { name: "sam_spawn_monsters", category: "Monsters", hostOnly: true,
    params: [{ name: "near_uid", type: "uid" }, { name: "monster_type", type: "string" }, { name: "count", type: "int" }],
    returns: "the number actually spawned (number)", desc: "Spawn `count` (1-8) monsters of a type near an anchor entity's UID." },
  { name: "sam_get_monster_target", category: "Monsters", hostOnly: false,
    params: [{ name: "uid", type: "uid" }],
    returns: "the targeted player index, or -1 (number)", desc: "Get the player index a monster is currently targeting (if any)." },
  { name: "sam_set_monster_target", category: "Monsters", hostOnly: true,
    params: [{ name: "uid", type: "uid" }, { name: "player", type: "int" }],
    returns: "true on success (boolean)", desc: "Make a monster acquire a player as its attack target." },
  { name: "sam_get_monster_data", category: "Monsters", hostOnly: false,
    params: [{ name: "uid", type: "uid" }, { name: "key", type: "string" }],
    returns: "the stored value, or nil/undefined", desc: "Read per-monster scratch data (boss phases, etc.); in-memory, cleared on shutdown." },
  { name: "sam_set_monster_data", category: "Monsters", hostOnly: false,
    params: [{ name: "uid", type: "uid" }, { name: "key", type: "string" }, { name: "value", type: "any" }],
    returns: "true on success (boolean)", desc: "Store any primitive/table value in a monster's scratch store (JSON-marshaled)." },

  // ---- Live patching ----------------------------------------------------------
  { name: "sam_patch_class", category: "Live patching", hostOnly: false,
    params: [{ name: "class", type: "any", values: ["classnum (int)", "\"namespace:class\" (string)"] }, { name: "patch", type: "table" }],
    returns: "true on success (boolean)", desc: "Override a class's STARTING stats/skills (patch = { STR, DEX, ..., MAXHP, skills = {...} }). Per-machine — call on every peer in multiplayer; reverts on unload." },
  { name: "sam_unpatch_class", category: "Live patching", hostOnly: false,
    params: [{ name: "class", type: "any", values: ["classnum (int)", "\"namespace:class\" (string)"] }],
    returns: "true on success (boolean)", desc: "Revert a class stat/skill patch." },
  { name: "sam_patch_item", category: "Live patching", hostOnly: false,
    params: [{ name: "item", type: "any", values: ["item id (int)", "vanilla name (string)", "\"ns:item\" (string)"] }, { name: "patch", type: "table" }],
    returns: "true on success (boolean)", desc: "Override an item type's base fields live: { weight, value/gold_value, level, category, slot, tooltip, name/name_identified, name_unidentified, attributes = {...} }." },
  { name: "sam_patch_monster", category: "Live patching", hostOnly: true,
    params: [{ name: "monster", type: "any", values: ["monster type id (int)", "monster type name (string)"] }, { name: "patch", type: "table" }],
    returns: "true if any field applied (boolean)", desc: "Override a monster type's base stats (e.g. { HP, MAXHP, STR }) for future spawns; also zero RANDOM_* for exact values." },
  { name: "sam_add_class_passive", category: "Live patching", hostOnly: true,
    params: [{ name: "class", type: "any", values: ["classnum (int)", "\"namespace:class\" (string)"] }, { name: "effect", type: "any", values: ["EFF_ id (int)", ...EFFECTS] }],
    returns: "true on success (boolean)", desc: "Grant a class a permanent status effect at character creation (bakes at creation; run at mod-load)." },
  { name: "sam_remove_class_passive", category: "Live patching", hostOnly: true,
    params: [{ name: "class", type: "any", values: ["classnum (int)", "\"namespace:class\" (string)"] }, { name: "effect", type: "any", values: ["EFF_ id (int)", ...EFFECTS] }],
    returns: "true on success (boolean)", desc: "Remove a class passive effect previously added." },

  // ---- Spells -----------------------------------------------------------------
  { name: "sam_grant_spell", category: "Spells", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "spell", type: "string", values: ["vanilla SPELL_ name", "\"namespace:spell\" (custom)"] }],
    returns: "true on success (boolean)", desc: "Grant a spell to a player: a vanilla SPELL_ name, or a custom \"namespace:spell\"." },
  { name: "sam_cast_spell", category: "Spells", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "spell", type: "string", values: ["vanilla SPELL_ name", "\"namespace:spell\" (custom)"] }],
    returns: "true if a projectile spawned (boolean)", desc: "Immediately FIRE a spell/bolt from a player in the direction they face (free, no mana). Great for 'shoot on block'. Don't call from an on_spell_cast handler." },
  { name: "sam_cast_spell_at", category: "Spells", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "target_uid", type: "uid" }, { name: "spell", type: "string", values: ["vanilla SPELL_ name", "\"namespace:spell\" (custom)"] }],
    returns: "the missile's uid (int), or nil/null", desc: "Fire a spell AIMED at an entity (aims the bolt toward it) instead of straight ahead. Free cast, host-only." },
  { name: "sam_cast_spell_pos", category: "Spells", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "tile_x", type: "int" }, { name: "tile_y", type: "int" }, { name: "spell", type: "string", values: ["vanilla SPELL_ name", "\"namespace:spell\" (custom)"] }],
    returns: "the missile's uid (int), or nil/null", desc: "Fire a spell aimed at a map tile. Free cast, host-only." },
  { name: "sam_monster_cast_spell", category: "Spells", hostOnly: true,
    params: [{ name: "uid", type: "uid" }, { name: "spell", type: "string", values: ["vanilla SPELL_ name", "\"namespace:spell\" (custom)"] }],
    returns: "the missile's uid (int), or nil/null", desc: "Make a monster (or a companion) cast a spell along its own facing. Free cast, host-only." },
  { name: "sam_get_spells", category: "Spells", hostOnly: false,
    params: [{ name: "player", type: "int" }],
    returns: "array/table of spell internal-name strings", desc: "List the spells a player currently knows." },
  { name: "sam_player_knows_spell", category: "Spells", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "spell", type: "string", values: ["vanilla SPELL_ name", "\"namespace:spell\" (custom)"] }],
    returns: "whether the player knows it (boolean)", desc: "Check whether a player already knows a spell (vanilla or custom)." },
  { name: "sam_remove_spell", category: "Spells", hostOnly: true,
    params: [{ name: "player", type: "int" }, { name: "spell", type: "string", values: ["vanilla SPELL_ name", "\"namespace:spell\" (custom)"] }],
    returns: "true if it was known and removed (boolean)", desc: "Un-learn a spell from a player's known list (local player). The counterpart to sam_grant_spell." },

  // ---- Persistence ------------------------------------------------------------
  { name: "sam_save_data", category: "Persistence", hostOnly: false,
    params: [{ name: "key", type: "string" }, { name: "value", type: "any" }],
    returns: "true on success (boolean)", desc: "Persist a value (number/string/bool/table) for the calling mod under savegames/sam_mod_data/<ns>/." },
  { name: "sam_load_data", category: "Persistence", hostOnly: false,
    params: [{ name: "key", type: "string" }],
    returns: "the stored value, or nil/undefined if unset", desc: "Read back a persisted per-mod value." },
  { name: "sam_delete_data", category: "Persistence", hostOnly: false,
    params: [{ name: "key", type: "string" }],
    returns: "true (boolean)", desc: "Delete a persisted per-mod key." },
  { name: "sam_set_player_data", category: "Persistence", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "key", type: "string" }, { name: "value", type: "any" }],
    returns: "nothing", desc: "Store a per-player value (number/string/bool/table) in memory for THIS session — the right tool for cooldowns, ability flags and stack counters you read often. Unlike sam_save_data it never touches disk and is cleared on a new game." },
  { name: "sam_get_player_data", category: "Persistence", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "key", type: "string" }],
    returns: "the stored value, or nil/undefined if unset", desc: "Read back a per-player in-memory value set by sam_set_player_data." },

  // ---- Timers -----------------------------------------------------------------
  { name: "sam_set_timer", category: "Timers", hostOnly: false,
    params: [{ name: "id", type: "string" }, { name: "delay_ticks", type: "int" }, { name: "callback", type: "any" }],
    returns: "nothing", desc: "Run `callback` (a function) once after delay_ticks (50/sec). Replaces any timer with the same id. Ticks host-side." },
  { name: "sam_set_repeating_timer", category: "Timers", hostOnly: false,
    params: [{ name: "id", type: "string" }, { name: "interval_ticks", type: "int" }, { name: "callback", type: "any" }],
    returns: "nothing", desc: "Run `callback` (a function) every interval_ticks until cancelled. Ticks host-side." },
  { name: "sam_cancel_timer", category: "Timers", hostOnly: false,
    params: [{ name: "id", type: "string" }],
    returns: "nothing", desc: "Cancel a pending timer by id (for the calling mod)." },

  // ---- Custom events ----------------------------------------------------------
  { name: "sam_register_hook", category: "Custom events", hostOnly: false,
    params: [{ name: "name", type: "string" }],
    returns: "nothing", desc: "Declare a namespaced custom hook. Name must contain a colon (\"namespace:hook_name\")." },
  { name: "sam_fire_hook", category: "Custom events", hostOnly: true,
    params: [{ name: "name", type: "string" }, { name: "event", type: "table", optional: true }],
    returns: "the number of scripts the event reached (number)", desc: "Fire a custom event to ALL Lua + JS/TS scripts cross-runtime. Only number/bool/string fields cross over; recursion capped at depth 8." },

  // ---- Input ------------------------------------------------------------------
  { name: "sam_is_key_held", category: "Input", hostOnly: false,
    params: [{ name: "key_name", type: "string", values: ["A-Z", "0-9", "F1-F12"] }],
    returns: "whether the key is down (boolean)", desc: "Check whether a supported RAW key is currently held (A-Z, 0-9, F1-F12). Ignores the player's keybinds — prefer sam_is_action_held, which follows them." },
  { name: "sam_is_action_held", category: "Input", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "action", type: "string", values: ACTIONS }],
    returns: "whether the action is active (boolean)",
    desc: "Check whether a BOUND action is held. Reads Barony's own binding, so it follows whatever the player rebound it to (and works with mouse buttons, which raw keys can't see). Local player only — input never leaves its machine." },
  { name: "sam_get_action_binding", category: "Input", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "action", type: "string", values: ACTIONS }],
    returns: "the physical input, e.g. \"Mouse3\" (string; nil/null if unbound)",
    desc: "What the player actually has an action bound to — use it to print a correct prompt instead of guessing a key." },

  // ---- Pictures (v1.10.3) ------------------------------------------------------
  // A mod's OWN art on screen. Name a picture three ways, in this order: "ns:name" from any
  // mod's manifest "images" list, a bare "name" meaning one of the CALLING mod's declared
  // images, or a path inside the calling mod's folder. Prefer the manifest forms — those are
  // checked when the mod loads, so a typo is a line in the log instead of nothing appearing.
  { name: "sam_show_image", category: "Pictures", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "image", type: "string" }, { name: "duration_ms", type: "int", optional: true }, { name: "alpha", type: "int", optional: true }, { name: "fit", type: "string", values: ["stretch", "contain"], optional: true }],
    returns: "true if the picture resolved (boolean)",
    desc: "Cover a player's screen with one of the mod's pictures, over the world AND the HUD, for duration_ms (0 or omitted = until sam_hide_image). This is the jumpscare / title-card / death-splash layer: it removes itself, so there is nothing to clean up. alpha is 0..255 (default 255). \"contain\" keeps the picture's aspect ratio; \"stretch\" (default) fills the view. In multiplayer the host forwards the image NAME to the owning client, which draws it from its own copy of the mod." },
  { name: "sam_show_image_at", category: "Pictures", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "image", type: "string" }, { name: "x", type: "int" }, { name: "y", type: "int" }, { name: "w", type: "int" }, { name: "h", type: "int" }, { name: "duration_ms", type: "int", optional: true }, { name: "alpha", type: "int", optional: true }],
    returns: "true if the picture resolved (boolean)",
    desc: "The same overlay, placed rather than full-screen. Coordinates are virtual screen pixels (the space sam_hud_text uses), so a fixed layout survives any resolution. w or h of 0 means the picture's own size on that axis. Still drawn over the HUD — for a picture that sits IN the HUD, use sam_hud_image." },
  { name: "sam_hide_image", category: "Pictures", hostOnly: false,
    params: [{ name: "player", type: "int", optional: true }],
    returns: "true if something was showing (boolean)",
    desc: "Take the overlay away early. No player clears every player's." },
  { name: "sam_hud_image", category: "Pictures", hostOnly: false,
    params: [{ name: "id", type: "string" }, { name: "x", type: "int" }, { name: "y", type: "int" }, { name: "w", type: "int" }, { name: "h", type: "int" }, { name: "image", type: "string" }, { name: "color", type: "int (0xRRGGBBAA)", optional: true }],
    returns: "true on success (boolean)",
    desc: "A PERSISTENT picture in the script HUD — a portrait, a custom gauge, a marker. Stays until sam_hud_clear(id) or the mod unloads, unlike the overlay. w/h of 0 means the picture's own pixel size. The colour is MIXED into the art, so white (the default) leaves it untouched and the alpha byte fades it." },
  { name: "sam_get_image_size", category: "Pictures", hostOnly: false,
    params: [{ name: "image", type: "string" }],
    returns: "width, height (two numbers in Lua; a [w, h] array in JS/TS; nil/null if it could not be loaded)",
    desc: "The picture's own pixel size, so a script can centre or scale it instead of hard-coding the numbers it was exported at. Also the cheapest way to check a picture actually resolves." },

  // ---- HUD ---------------------------------------------------------------------
  // Widgets persist until changed or cleared, so update them on an event rather than every
  // frame. Coordinates are virtual screen pixels, the same space the vanilla HUD uses.
  { name: "sam_hud_text", category: "HUD", hostOnly: false,
    params: [{ name: "id", type: "string" }, { name: "x", type: "int" }, { name: "y", type: "int" }, { name: "text", type: "string" }, { name: "color", type: "int (0xRRGGBBAA)", optional: true }],
    returns: "true on success (boolean)",
    desc: "Show or update a line of text on screen. Calling again with the same id moves/retitles the existing line rather than stacking a new one." },
  { name: "sam_hud_bar", category: "HUD", hostOnly: false,
    params: [{ name: "id", type: "string" }, { name: "x", type: "int" }, { name: "y", type: "int" }, { name: "w", type: "int" }, { name: "h", type: "int" }, { name: "frac", type: "number (0..1)" }, { name: "color", type: "int (0xRRGGBBAA)", optional: true }],
    returns: "true on success (boolean)",
    desc: "Show or update a horizontal bar — a custom resource, a charge meter, a boss health track. frac is clamped to 0..1; 0 draws as empty rather than a sliver." },
  { name: "sam_hud_clear", category: "HUD", hostOnly: false,
    params: [{ name: "id", type: "string", optional: true }],
    returns: "true if that id was showing (boolean)",
    desc: "Remove one HUD element. No id removes the whole script HUD. The HUD is also dropped automatically when the mod unloads, so it can never outlive the mod that drew it." },

  // ---- Terrain and space (v1.10.3) ---------------------------------------------
  { name: "sam_get_tile", category: "Terrain", hostOnly: false,
    params: [{ name: "x", type: "int" }, { name: "y", type: "int" }],
    returns: "table { wall, floor, ceiling, solid, water, lava, walkable } or nil for a tile off the map",
    desc: "Read one map tile. Liquid comes from the FLOOR tile, and the engine decides which tiles are liquid from their image filename — so a mod's own tile named \"...lava...\" reports as lava here too." },
  { name: "sam_set_tile", category: "Terrain", hostOnly: true,
    params: [{ name: "x", type: "int" }, { name: "y", type: "int" }, { name: "layer", type: "int (0=floor, 1=wall, 2=ceiling)" }, { name: "tileId", type: "int" }],
    returns: "true on success (boolean)",
    desc: "Write one map tile — dig a passage, wall something in, flood a room. Refuses out of bounds rather than corrupting the map array. Check sam_tiles_connected afterwards if the edit could seal the exit." },
  { name: "sam_is_spawnable", category: "Terrain", hostOnly: false,
    params: [{ name: "x", type: "int" }, { name: "y", type: "int" }],
    returns: "boolean",
    desc: "Is this a sane place to put something: in bounds, not inside a wall, not lava. Check before spawning instead of dropping a monster into rock." },
  { name: "sam_line_of_sight", category: "Terrain", hostOnly: false,
    params: [{ name: "x1", type: "int" }, { name: "y1", type: "int" }, { name: "x2", type: "int" }, { name: "y2", type: "int" }, { name: "blockedByEntities", type: "boolean", optional: true }],
    returns: "visible, blockedX, blockedY (blocked coords are -1 when visible)",
    desc: "Can a straight line get from A to B? This is the engine's own trace, so it agrees with what is drawn — unlike plain distance, which sees through solid rock." },
  { name: "sam_tiles_connected", category: "Terrain", hostOnly: false,
    params: [{ name: "x1", type: "int" }, { name: "y1", type: "int" }, { name: "x2", type: "int" }, { name: "y2", type: "int" }, { name: "flying", type: "boolean", optional: true }],
    returns: "boolean",
    desc: "Can something WALK (or fly) from A to B at all? The softlock check: after a mod edits terrain, ask whether the exit is still reachable before committing." },
  { name: "sam_get_light_at", category: "Terrain", hostOnly: false,
    params: [{ name: "x", type: "int" }, { name: "y", type: "int" }, { name: "player", type: "int", optional: true }],
    returns: "0..255",
    desc: "How lit a tile is, computed exactly the way the engine computes it, so the number you get back is the number monster vision thresholds on rather than an approximation of it. Barony keeps one SHARED lightmap holding light that is there for everyone (a wall torch, a lit room) plus one per camera that also holds that player's own glow. This reads the shared one by default, because that is the one the AI reads. Pass a player index if you want what that player's screen actually shows instead." },
  { name: "sam_find_entities", category: "Terrain", hostOnly: false,
    params: [{ name: "x", type: "int" }, { name: "y", type: "int" }, { name: "radiusTiles", type: "number" }, { name: "kind", type: "string", values: ["any", "door", "chest", "fountain", "sink", "switch", "gate", "ladder", "portal", "item", "gold", "boulder", "monster", "player"], optional: true }],
    returns: "array of uids",
    desc: "Entities of a KIND near a tile. This is the gap sam_get_nearby_entities leaves: that one skips anything which is not a monster or a player, so doors, chests, levers, gold and dropped items were invisible to scripts." },
  { name: "sam_get_container_items", category: "Terrain", hostOnly: true,
    params: [{ name: "uid", type: "uid" }],
    returns: "array of tables { type, name, count, status, beatitude, identified } or nil",
    desc: "What is inside a chest, or what a creature is carrying." },

  // ---- Mechanisms (v1.10.3) ----------------------------------------------------
  // The framework already FIRED world.on_door_opened and world.on_switch_toggled while giving
  // a script no verb to open a door or throw a lever. These close that.
  { name: "sam_set_door", category: "Mechanisms", hostOnly: true,
    params: [{ name: "uid", type: "uid" }, { name: "open", type: "boolean" }],
    returns: "true on success (boolean)",
    desc: "Open or close a door. Find one with sam_find_entities(x, y, r, \"door\")." },
  { name: "sam_set_door_locked", category: "Mechanisms", hostOnly: true,
    params: [{ name: "uid", type: "uid" }, { name: "locked", type: "boolean" }],
    returns: "true on success (boolean)",
    desc: "Lock or unlock a door." },
  { name: "sam_power_entity", category: "Mechanisms", hostOnly: true,
    params: [{ name: "uid", type: "uid" }, { name: "on", type: "boolean" }],
    returns: "true on success (boolean)",
    desc: "Power a mechanism on or off, as a switch wired to it would." },
  { name: "sam_toggle_switch", category: "Mechanisms", hostOnly: true,
    params: [{ name: "uid", type: "uid" }],
    returns: "true on success (boolean)",
    desc: "Flip a lever or switch, driving whatever it is wired to." },

  // ---- Level and mod context (v1.10.3) -----------------------------------------
  { name: "sam_get_level_info", category: "Context", hostOnly: false,
    params: [],
    returns: "table { floor, name, author, width, height, secret, skybox, no_digging, no_teleport, no_levitation }",
    desc: "Everything about the current floor. sam_get_floor returns a bare number that cannot tell a secret branch from the main one, so location-gated content was impossible before this." },
  { name: "sam_get_mods", category: "Context", hostOnly: false,
    params: [],
    returns: "array of { ns, name, version, author }",
    desc: "Every S.A.M mod loaded right now. Cross-mod integration with zero engine work: soft-depend on another mod, avoid double registering, or light up extra content when a partner mod is present." },
  { name: "sam_is_mod_loaded", category: "Context", hostOnly: false,
    params: [{ name: "namespace", type: "string" }],
    returns: "boolean",
    desc: "Is a given mod namespace loaded? The cheap form of sam_get_mods." },

  // ---- Reading the truth (v1.10.3) ---------------------------------------------
  // These take a UID, never a player index. The older API is split (sam_get_stat wants an
  // index, sam_get_monster_stat wants a uid) and that mismatch fails SILENTLY — a uid passed
  // where an index belongs falls out of range and returns nothing. Call sam_get_player_uid(n)
  // once and pass uids from there.
  { name: "sam_get_effective_stat", category: "Truth", hostOnly: false,
    params: [{ name: "uid", type: "uid" }, { name: "stat", type: "string", values: PLAYER_STATS }],
    returns: "number",
    desc: "A stat as the game actually uses it — gear, effects and curses folded in — rather than the raw number on the sheet." },
  { name: "sam_get_ac", category: "Truth", hostOnly: false,
    params: [{ name: "uid", type: "uid" }],
    returns: "number",
    desc: "Armor class as the damage formula sees it, gear included." },
  { name: "sam_get_skill", category: "Truth", hostOnly: false,
    params: [{ name: "uid", type: "uid" }, { name: "skill", type: "string" }, { name: "effective", type: "boolean", optional: true }],
    returns: "0..100",
    desc: "A proficiency rank. Accepts both spellings — \"PRO_SWORD\" (the class schema) and \"sword\" (what player.on_proficiency_increased hands you). effective (default true) includes the equipment bonus the game actually uses; pass false for the raw trained rank. Ranks were completely unreadable before this, even though the framework has always fired the event." },
  { name: "sam_is_enemy", category: "Truth", hostOnly: false,
    params: [{ name: "uid_a", type: "uid" }, { name: "uid_b", type: "uid" }],
    returns: "boolean",
    desc: "Would these two fight? The engine's own allegiance answer, so charm, race and faction are all accounted for." },
  { name: "sam_is_friend", category: "Truth", hostOnly: false,
    params: [{ name: "uid_a", type: "uid" }, { name: "uid_b", type: "uid" }],
    returns: "boolean",
    desc: "The other side of sam_is_enemy — allies, followers and charmed creatures." },
  { name: "sam_get_monster_type", category: "Truth", hostOnly: false,
    params: [{ name: "uid", type: "uid" }],
    returns: "the species name, e.g. \"skeleton\" (string; nil if not a creature)",
    desc: "Identify a creature by name instead of the raw integer in an event payload. NOTE this is the BASE type: a custom monster is a variant of a vanilla species, so a mod's \"Rathalos\" built on a bat answers \"bat\". Use sam_get_monster_name for the variant's own name, or sam_monster_has_trait to tell modded creatures apart." },
  { name: "sam_get_monster_name", category: "Truth", hostOnly: false,
    params: [{ name: "uid", type: "uid" }],
    returns: "the creature's DISPLAY name (string; nil if not a creature)",
    desc: "For a mod's custom monster this is the variant name it was given (\"Rathalos\"). A plain vanilla creature carries an empty variant name, so this falls back to the species name and never hands a script an empty string." },
  { name: "sam_monster_has_trait", category: "Truth", hostOnly: false,
    params: [{ name: "uid", type: "uid" }, { name: "trait", type: "string" }],
    returns: "boolean",
    desc: "Reads back what the mod declared in JSON. Without this a mod can SAY a monster is undead and the engine agrees, but the mod's own script cannot ask — so a \"bonus vs undead\" rule had no way to test for undead. False for every vanilla monster, so it is a no-op without a mod." },

  // ---- Presentation (v1.6.0 / v1.10.3) -----------------------------------------
  { name: "sam_spawn_particle", category: "Presentation", hostOnly: true,
    params: [{ name: "kind", type: "string", values: ["poof", "explosion", "bang", "sleep"] }, { name: "tileX", type: "int" }, { name: "tileY", type: "int" }, { name: "z", type: "number", optional: true }, { name: "scale", type: "number", optional: true }],
    returns: "true on success (boolean)",
    desc: "A vanilla particle burst at a tile, so a mod's own effect looks like part of the game." },
  { name: "sam_play_sound_at", category: "Presentation", hostOnly: true,
    params: [{ name: "sound", type: "int | string (\"ns:sound\")" }, { name: "tileX", type: "int" }, { name: "tileY", type: "int" }, { name: "volume", type: "int", optional: true }],
    returns: "true on success (boolean)",
    desc: "Positional audio: it attenuates with distance and pans, so a trap firing across the level is quiet, and in co-op each player hears it from where THEY are." },
  { name: "sam_play_sound_entity", category: "Presentation", hostOnly: true,
    params: [{ name: "sound", type: "int | string (\"ns:sound\")" }, { name: "uid", type: "uid" }, { name: "volume", type: "int", optional: true }],
    returns: "true on success (boolean)",
    desc: "The same, but the sound follows the entity as it moves." },
  { name: "sam_damage_number", category: "Presentation", hostOnly: true,
    params: [{ name: "uid", type: "uid" }, { name: "amount", type: "int" }, { name: "type", type: "int", optional: true }],
    returns: "true on success (boolean)",
    desc: "The floating combat number the game shows on a hit. Lets a mod's custom damage read like real damage instead of being invisible." },
  { name: "sam_screen_flash", category: "Presentation", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "r", type: "int" }, { name: "g", type: "int" }, { name: "b", type: "int" }, { name: "intensity", type: "number (0..1)", optional: true }, { name: "duration_ms", type: "int", optional: true }],
    returns: "true if accepted (boolean)",
    desc: "Flash a player's whole screen in an RGB colour that fades to nothing — the anime \"impact frame\". intensity 0..1 is the peak opacity. Drawn on the machine the player lives on." },
  { name: "sam_impact_frame", category: "Presentation", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "r", type: "int" }, { name: "g", type: "int" }, { name: "b", type: "int" }, { name: "intensity", type: "number (0..1)", optional: true }, { name: "duration_ms", type: "int", optional: true }, { name: "lines", type: "int", optional: true }],
    returns: "true if accepted (boolean)",
    desc: "The EXAGGERATED version of the flash: a colour pop PLUS manga speed lines converging on screen centre PLUS a bright core flare. Pair it with sam_camera_shake and sam_hitstop for a full impact beat. lines is the speed-line count (0 = a plain flash)." },
  { name: "sam_camera_shake", category: "Presentation", hostOnly: false,
    params: [{ name: "player", type: "int" }, { name: "magnitude", type: "number (~1..20)" }],
    returns: "true if accepted (boolean)",
    desc: "Shake a player's camera. 1 is a nudge, ~10 a solid hit, 20+ violent. Feeds Barony's own shake channels so it decays naturally; for a remote client the host forwards it." },
  { name: "sam_hitstop", category: "Presentation", hostOnly: true,
    params: [{ name: "duration_ms", type: "int" }],
    returns: "true if accepted (boolean)",
    desc: "Briefly freeze enemy and projectile logic — a freeze-frame — for duration_ms (capped ~400). The player, HUD weapon and hand magic keep animating, so it reads as a punchy impact beat. SINGLEPLAYER ONLY: freezing host logic in a netgame would desync clients." },

  // ---- Modifying a value mid-hook ----------------------------------------------
  { name: "sam_modify_value", category: "Hooks", hostOnly: true,
    params: [{ name: "newValue", type: "number" }],
    returns: "true on success (boolean)",
    desc: "Rewrite the number the engine is about to use, from inside any hook that offers one (XP gained, gold gained, and every future modifiable hook). Only valid inside such a callback — the error names the hook you ARE inside, so a wrong-place call says something useful." },
  { name: "sam_modify_monster_damage", category: "Hooks", hostOnly: true,
    params: [{ name: "newValue", type: "number" }],
    returns: "true on success (boolean)",
    desc: "Rewrite the damage a MONSTER is about to take. Only valid inside an on_before_monster_damage callback. No subject argument: only one monster is ever mid-dispatch." },

  // ---- Networking --------------------------------------------------------------
  { name: "sam_send_packet", category: "Networking", hostOnly: false,
    params: [{ name: "target", type: "int (player 0..3, or -1 for every client)" }, { name: "tag", type: "string" }, { name: "payload", type: "string" }],
    returns: "true if sent (boolean)",
    desc: "Send a mod-defined message to another machine. Barony's packet ids are a fixed table, so before this a co-op mod had no way to tell the other side anything at all. On a client the target is ignored and the packet always goes to the host. The other side receives an \"on_packet\" event with .from, .tag and .payload. One datagram only — use sam_save_data for bulk state." },
];

export const SAM_EVENTS = [
  // ---- Core player / combat (v0.3-v0.5) ---------------------------------------
  { name: "player.on_before_item_pickup", category: "player", cancellable: true,
    whenFired: "before an item the player walked over enters the inventory",
    payload: [{ field: "player", type: "int" }, { field: "item_type", type: "int" }, { field: "item_count", type: "int" }, { field: "category", type: "int" }],
    notes: "Return false to refuse the pickup; the item stays on the floor. Only fires for genuine world pickups, never for starting gear or internal grants." },
  { name: "world.on_item_deployed", category: "world", cancellable: true,
    whenFired: "a thrown gadget lands and something must be built there",
    payload: [{ field: "item_type", type: "int" }, { field: "player", type: "int" }, { field: "x", type: "int" }, { field: "y", type: "int" }, { field: "status", type: "int" }, { field: "beatitude", type: "int" }],
    notes: "Return false after spawning your own thing, to skip the engine's built-in gadget list. This is how a mod makes custom traps and turrets." },
  { name: "on_before_monster_damage", category: "combat", cancellable: false,
    whenFired: "before a monster's HP is reduced",
    payload: [{ field: "monster_uid", type: "int" }, { field: "monster_type", type: "int" }, { field: "damage", type: "int" }],
    notes: "Rewrite the number with sam_modify_monster_damage(n). Set 0 to negate the hit entirely. Not cancellable by returning false." },
  { name: "player.on_level_up", category: "player", cancellable: false,
    whenFired: "a player gains a level",
    payload: [{ field: "player", type: "int" }, { field: "level", type: "int" }, { field: "amount", type: "int" }, { field: "stats", type: "int" }],
    gotcha: "" },
  { name: "player.on_hit", category: "combat", cancellable: false,
    whenFired: "a player's melee weapon hits an entity",
    payload: [{ field: "player", type: "int" }, { field: "target_uid", type: "uid" }, { field: "target_type", type: "int" }, { field: "damage", type: "int" }, { field: "weapon_type", type: "int" }, { field: "lethal", type: "int" }],
    gotcha: "melee only; lethal is 0/1" },
  { name: "player.on_kill", category: "combat", cancellable: false,
    whenFired: "a player's melee blow kills an entity",
    payload: [{ field: "player", type: "int" }, { field: "target_uid", type: "uid" }, { field: "target_type", type: "int" }, { field: "was_lethal", type: "int" }],
    gotcha: "melee only; for ranged/magic kills use on_monster_died and its killer_uid" },
  { name: "player.on_damage_taken", category: "combat", cancellable: false,
    whenFired: "a player takes damage from any source",
    payload: [{ field: "player", type: "int" }, { field: "damage", type: "int" }, { field: "hp", type: "int" }, { field: "maxhp", type: "int" }, { field: "lethal", type: "int" }, { field: "source_uid", type: "uid" }, { field: "source_type", type: "int" }],
    gotcha: "fires AFTER HP is already gone; cannot cancel — use on_before_damage to reduce/cancel" },
  { name: "player.on_death", category: "combat", cancellable: false,
    whenFired: "a player dies",
    payload: [{ field: "player", type: "int" }, { field: "killer_type", type: "int" }, { field: "killer_uid", type: "uid" }, { field: "killer_monster", type: "int" }, { field: "obituary", type: "string" }],
    gotcha: "" },
  { name: "player.on_equip", category: "player", cancellable: false,
    whenFired: "a player equips an item",
    payload: [{ field: "player", type: "int" }, { field: "item_type", type: "int" }, { field: "slot", type: "string" }],
    gotcha: "slot is a lowercase name, e.g. \"cloak\"" },
  { name: "player.on_unequip", category: "player", cancellable: false,
    whenFired: "a player unequips an item",
    payload: [{ field: "player", type: "int" }, { field: "item_type", type: "int" }, { field: "item_count", type: "int" }, { field: "slot", type: "string" }],
    gotcha: "" },
  { name: "player.on_item_use", category: "player", cancellable: true,
    whenFired: "a player uses a consumable (potion / scroll / food)",
    payload: [{ field: "player", type: "int" }, { field: "item_type", type: "int" }, { field: "item_count", type: "int" }, { field: "category", type: "string" }],
    gotcha: "" },
  { name: "player.on_spell_cast", category: "player", cancellable: true,
    whenFired: "a player casts a spell",
    payload: [{ field: "player", type: "int" }, { field: "spell_id", type: "int" }, { field: "spell_name", type: "string" }, { field: "target_uid", type: "uid" }],
    gotcha: "" },
  { name: "player.on_gold_collected", category: "player", cancellable: false,
    whenFired: "a player picks up gold",
    payload: [{ field: "player", type: "int" }, { field: "amount", type: "int" }, { field: "total_gold", type: "int" }],
    gotcha: "" },
  { name: "player.on_floor_change", category: "world", cancellable: false,
    whenFired: "a player descends to a new floor",
    payload: [{ field: "player", type: "int" }, { field: "old_floor", type: "int" }, { field: "new_floor", type: "int" }],
    gotcha: "good place to reset per-floor sam_save_data state" },

  // ---- Combat (v0.6.0) --------------------------------------------------------
  { name: "player.on_attack_start", category: "combat", cancellable: false,
    whenFired: "a player starts an attack swing (any weapon)",
    payload: [{ field: "player", type: "int" }, { field: "weapon_type", type: "int" }, { field: "target_uid", type: "uid" }],
    gotcha: "" },
  { name: "player.on_block", category: "combat", cancellable: false,
    whenFired: "a player blocks a hit while defending with a shield (partial or full)",
    payload: [{ field: "player", type: "int" }, { field: "shield_type", type: "int" }, { field: "full_block", type: "int" }, { field: "damage_taken", type: "int" }, { field: "attacker_uid", type: "uid" }, { field: "attacker_type", type: "int" }, { field: "damage_blocked", type: "int" }],
    gotcha: "Fires on ANY block while actively defending with a shield and getting hit — not on right-click alone. full_block is 1 when all damage was negated (0 on a partial block); damage_taken is what leaked through. shield_type is the blocking shield's item id; gate with sam_item_id(\"namespace:item\") to react only to your own shield." },
  { name: "player.on_miss", category: "combat", cancellable: false,
    whenFired: "a player's melee swing connects with nothing",
    payload: [{ field: "player", type: "int" }, { field: "target_uid", type: "uid" }, { field: "weapon_type", type: "int" }],
    gotcha: "" },
  { name: "player.on_bleed_tick", category: "combat", cancellable: false,
    whenFired: "bleeding ticks damage on a player",
    payload: [{ field: "player", type: "int" }, { field: "damage", type: "int" }, { field: "stacks_remaining", type: "int" }],
    gotcha: "" },
  { name: "player.on_poison_tick", category: "combat", cancellable: false,
    whenFired: "poison ticks damage on a player",
    payload: [{ field: "player", type: "int" }, { field: "damage", type: "int" }, { field: "stacks_remaining", type: "int" }],
    gotcha: "" },

  // ---- Items & inventory (v0.6.0) ---------------------------------------------
  { name: "player.on_item_identified", category: "player", cancellable: false,
    whenFired: "a player identifies an item",
    payload: [{ field: "player", type: "int" }, { field: "item_type", type: "int" }, { field: "item_name", type: "string" }],
    gotcha: "" },
  { name: "player.on_item_dropped", category: "player", cancellable: false,
    whenFired: "a player drops an item",
    payload: [{ field: "player", type: "int" }, { field: "item_type", type: "int" }, { field: "floor_x", type: "int" }, { field: "floor_y", type: "int" }],
    gotcha: "" },
  { name: "player.on_item_broken", category: "player", cancellable: false,
    whenFired: "a player's equipped item breaks",
    payload: [{ field: "player", type: "int" }, { field: "item_type", type: "int" }, { field: "slot", type: "string" }],
    gotcha: "" },
  { name: "player.on_chest_opened", category: "player", cancellable: false,
    whenFired: "a player opens a chest",
    payload: [{ field: "player", type: "int" }, { field: "chest_uid", type: "uid" }, { field: "floor_x", type: "int" }, { field: "floor_y", type: "int" }],
    gotcha: "" },
  { name: "player.on_shop_entered", category: "player", cancellable: false,
    whenFired: "a player opens trade with a shopkeeper",
    payload: [{ field: "player", type: "int" }, { field: "shopkeeper_uid", type: "uid" }],
    gotcha: "" },
  { name: "player.on_item_bought", category: "player", cancellable: false,
    whenFired: "a player buys from a shop",
    payload: [{ field: "player", type: "int" }, { field: "item_type", type: "int" }, { field: "gold_spent", type: "int" }],
    gotcha: "" },
  { name: "player.on_item_sold", category: "player", cancellable: false,
    whenFired: "a player sells to a shop",
    payload: [{ field: "player", type: "int" }, { field: "item_type", type: "int" }, { field: "gold_received", type: "int" }],
    gotcha: "" },

  // ---- Magic & effects (v0.6.0) -----------------------------------------------
  { name: "player.on_spell_learned", category: "player", cancellable: false,
    whenFired: "a player learns a spell",
    payload: [{ field: "player", type: "int" }, { field: "spell_id", type: "int" }, { field: "spell_name", type: "string" }],
    gotcha: "" },
  { name: "player.on_spell_failed", category: "player", cancellable: false,
    whenFired: "a cast fizzles or is blocked",
    payload: [{ field: "player", type: "int" }, { field: "spell_id", type: "int" }, { field: "spell_name", type: "string" }, { field: "reason", type: "string" }],
    gotcha: "" },
  { name: "player.on_effect_applied", category: "player", cancellable: false,
    whenFired: "a status effect is newly applied to a player (a genuine off→on transition, not a refresh)",
    payload: [{ field: "player", type: "int" }, { field: "effect_name", type: "string" }, { field: "duration_ticks", type: "int" }, { field: "strength", type: "int" }],
    gotcha: "Re-applying an already-active effect does NOT re-fire this. To watch a refresh or stack change, poll sam_get_effect_strength / sam_get_effect_duration in on_tick." },
  { name: "player.on_effect_removed", category: "player", cancellable: false,
    whenFired: "a status effect ends (cleared or expired)",
    payload: [{ field: "player", type: "int" }, { field: "effect_name", type: "string" }, { field: "effect", type: "int" }],
    gotcha: "the numeric `effect` id is only present on expiry" },

  // ---- World & exploration (v0.6.0) -------------------------------------------
  { name: "world.on_trap_triggered", category: "world", cancellable: false,
    whenFired: "an arrow / spike / magic trap fires",
    payload: [{ field: "trap_type", type: "int" }, { field: "player", type: "int" }, { field: "floor_x", type: "int" }, { field: "floor_y", type: "int" }, { field: "damage", type: "int" }, { field: "spell", type: "int" }],
    gotcha: "carries `damage` for physical traps OR `spell` (spell id) for magic traps, not both" },
  { name: "world.on_monster_spawned", category: "world", cancellable: false,
    whenFired: "a monster is summoned at runtime",
    payload: [{ field: "monster_uid", type: "uid" }, { field: "monster_type", type: "int" }, { field: "monster_name", type: "string" }, { field: "floor_x", type: "int" }, { field: "floor_y", type: "int" }, { field: "floor", type: "int" }],
    gotcha: "" },
  { name: "world.on_chest_found", category: "world", cancellable: false,
    whenFired: "a player first walks up close to a chest (proximity — fires once per chest, NOT on opening it)",
    payload: [{ field: "player", type: "int" }, { field: "chest_uid", type: "uid" }, { field: "floor_x", type: "int" }, { field: "floor_y", type: "int" }],
    gotcha: "This is \"spotted a chest nearby\", not \"opened a chest\". For the moment a player actually opens one, use player.on_chest_opened." },
  { name: "world.on_boulder_triggered", category: "world", cancellable: false,
    whenFired: "a boulder trap launches",
    payload: [{ field: "floor_x", type: "int" }, { field: "floor_y", type: "int" }],
    gotcha: "" },

  // ---- Player state (v0.6.0) --------------------------------------------------
  { name: "player.on_hunger_change", category: "player", cancellable: false,
    whenFired: "hunger crosses a tier edge",
    payload: [{ field: "player", type: "int" }, { field: "hunger", type: "int" }, { field: "hunger_level", type: "int" }, { field: "old_hunger_level", type: "int" }],
    gotcha: "" },
  { name: "player.on_xp_gained", category: "player", cancellable: false,
    whenFired: "a player gains XP from a kill",
    payload: [{ field: "player", type: "int" }, { field: "amount", type: "int" }, { field: "source_type", type: "string" }, { field: "monster_type", type: "int" }],
    gotcha: "" },
  { name: "player.on_proficiency_increased", category: "player", cancellable: false,
    whenFired: "a skill rank goes up",
    payload: [{ field: "player", type: "int" }, { field: "proficiency", type: "int" }, { field: "proficiency_name", type: "string" }, { field: "old_rank", type: "int" }, { field: "new_rank", type: "int" }],
    gotcha: "" },
  { name: "player.on_status_effect_tick", category: "player", cancellable: false,
    whenFired: "an active status effect ticks",
    payload: [{ field: "player", type: "int" }, { field: "effect", type: "int" }, { field: "effect_name", type: "string" }, { field: "ticks_remaining", type: "int" }],
    gotcha: "throttled to ~1/sec" },

  // ---- Multiplayer & lifecycle (v0.6.0) ---------------------------------------
  { name: "player.on_player_joined", category: "lifecycle", cancellable: false,
    whenFired: "a client joins the lobby",
    payload: [{ field: "player_index", type: "int" }, { field: "player_name", type: "string" }, { field: "class_id", type: "int" }, { field: "race", type: "int" }],
    gotcha: "uses player_index, not player" },
  { name: "player.on_player_left", category: "lifecycle", cancellable: false,
    whenFired: "a client disconnects or times out",
    payload: [{ field: "player_index", type: "int" }, { field: "player_name", type: "string" }],
    gotcha: "uses player_index, not player" },
  { name: "player.on_player_revived", category: "lifecycle", cancellable: false,
    whenFired: "a downed player is revived on a new floor",
    payload: [{ field: "player", type: "int" }, { field: "revived_by", type: "int" }, { field: "floor", type: "int" }, { field: "revive_type", type: "int" }],
    gotcha: "" },
  { name: "game.on_level_entered", category: "lifecycle", cancellable: false,
    whenFired: "a floor finishes loading",
    payload: [{ field: "player", type: "int" }, { field: "floor", type: "int" }, { field: "level_name", type: "string" }],
    gotcha: "" },
  { name: "game.on_game_start", category: "lifecycle", cancellable: false,
    whenFired: "a new game begins",
    payload: [{ field: "player", type: "int" }, { field: "class_id", type: "int" }, { field: "class_name", type: "string" }, { field: "race", type: "int" }, { field: "race_name", type: "string" }],
    gotcha: "" },
  { name: "game.on_game_end", category: "lifecycle", cancellable: false,
    whenFired: "the game is won or the party wipes",
    payload: [{ field: "player", type: "int" }, { field: "won", type: "int" }, { field: "floor_reached", type: "int" }, { field: "kills", type: "int" }, { field: "time_played", type: "int" }],
    gotcha: "won is 0/1" },

  // ---- v0.7.0 hooks -----------------------------------------------------------
  { name: "on_tick", category: "tick", cancellable: false,
    whenFired: "every game tick (50/sec), for every script that defines on_tick(event)",
    payload: [{ field: "tick_count", type: "int" }, { field: "delta_ticks", type: "int" }],
    gotcha: "no player. prefix; delivered to the separate on_tick(event) handler, not on_event; host-only and silent" },
  { name: "on_before_damage", category: "combat", cancellable: false,
    whenFired: "before a player's HP is reduced (bracketed around Entity::modHP)",
    payload: [{ field: "player", type: "int" }, { field: "damage", type: "int" }],
    gotcha: "no player. prefix; the ONLY cancellable event — call sam_modify_damage(player, new) to reduce/cancel the incoming hit" },
  { name: "on_action_pressed", category: "input", cancellable: false,
    whenFired: "a BOUND action goes down — e.g. the player presses whatever they have \"Use\" mapped to",
    payload: [{ field: "player", type: "int" }, { field: "action", type: "string" }, { field: "binding", type: "string" }],
    gotcha: "no player. prefix. This is the one to use for button-mapped abilities: it reads Barony's named actions, so it follows the player's own keybinds and can never collide with them (it claims no key of its own), and it sees mouse buttons — which the raw-key hooks cannot. Observation only, so vanilla blocking/attacking/hotbar keep working. `binding` is the physical input (\"Mouse3\") for prompts, and is only filled in for the local player. Single-fire per press; use sam_is_action_held for continuous checks. In multiplayer a client's press is forwarded to the host, so the hook fires host-side where host-only APIs work." },
  { name: "on_action_released", category: "input", cancellable: false,
    whenFired: "a bound action goes back up",
    payload: [{ field: "player", type: "int" }, { field: "action", type: "string" }, { field: "binding", type: "string" }],
    gotcha: "no player. prefix; the release twin of on_action_pressed" },
  { name: "on_key_pressed", category: "input", cancellable: false,
    whenFired: "a supported RAW key transitions to down (A-Z, 0-9, F1-F12)",
    payload: [{ field: "player", type: "int" }, { field: "key_name", type: "string" }, { field: "held", type: "int" }],
    gotcha: "no player. prefix; single-fire per press — use sam_is_key_held for continuous checks. Reads the PHYSICAL key and ignores the player's keybinds, so it can collide with whatever they've bound there, and it can't see mouse buttons — prefer on_action_pressed unless you specifically want a raw key." },
  { name: "on_key_released", category: "input", cancellable: false,
    whenFired: "a supported key transitions to up (A-Z, 0-9, F1-F12)",
    payload: [{ field: "player", type: "int" }, { field: "key_name", type: "string" }],
    gotcha: "no player. prefix; no `held` field (present only on press)" },
  { name: "on_monster_damaged", category: "combat", cancellable: false,
    whenFired: "any monster takes damage",
    payload: [{ field: "monster_uid", type: "uid" }, { field: "monster_type", type: "int" }, { field: "damage", type: "int" }, { field: "hp", type: "int" }, { field: "max_hp", type: "int" }, { field: "killer_uid", type: "uid" }, { field: "floor", type: "int" }],
    gotcha: "no player. prefix; use killer_uid to identify the attacker (0 = environmental)" },
  { name: "on_monster_died", category: "combat", cancellable: false,
    whenFired: "any monster dies (melee, ranged, magic, or scripted)",
    payload: [{ field: "monster_uid", type: "uid" }, { field: "monster_type", type: "int" }, { field: "hp", type: "int" }, { field: "max_hp", type: "int" }, { field: "killer_uid", type: "uid" }, { field: "floor", type: "int" }],
    gotcha: "no player. prefix; the reliable kill hook for ranged/magic (player.on_kill is melee-only). killer_uid > 0 means an actual killer (skip traps/starvation)" },

  // ---- World interactions (v1.2.5) --------------------------------------------
  { name: "player.on_item_pickup", category: "player", cancellable: false,
    whenFired: "a player picks an item up off the ground",
    payload: [{ field: "player", type: "int" }, { field: "item_type", type: "int" }, { field: "count", type: "int" }, { field: "item_name", type: "string" }],
    gotcha: "in-world pickups only — does NOT fire for starting inventory. gate with sam_item_id(\"ns:item\") == event.item_type to react to your own item" },
  { name: "world.on_door_opened", category: "world", cancellable: false,
    whenFired: "a player opens a wooden door",
    payload: [{ field: "player", type: "int" }, { field: "door", type: "uid" }, { field: "status", type: "int" }, { field: "type", type: "string" }],
    gotcha: "opens only (not closes); wooden doors only, not iron gates. status is the swing direction (1 or 2)" },
  { name: "world.on_projectile_hit", category: "world", cancellable: false,
    whenFired: "a fired projectile strikes an entity",
    payload: [{ field: "player", type: "int" }, { field: "shooter_uid", type: "uid" }, { field: "target_uid", type: "uid" }, { field: "target_type", type: "int" }],
    gotcha: "player is the shooter's index, or -1 when a monster or trap fired it. target_type is the struck monster's type, or -1 for a non-creature (chest, etc.)" },
  { name: "world.on_fountain_used", category: "world", cancellable: false,
    whenFired: "a player drinks from / uses a fountain",
    payload: [{ field: "player", type: "int" }, { field: "fountain", type: "uid" }, { field: "effect", type: "int" }],
    gotcha: "fires once, just before the fountain dries up. effect is the fountain's rolled effect type" },
  { name: "world.on_sink_used", category: "world", cancellable: false,
    whenFired: "a player uses a sink",
    payload: [{ field: "player", type: "int" }, { field: "sink", type: "uid" }, { field: "outcome_code", type: "int" }, { field: "outcome", type: "string" }],
    gotcha: "outcome is one of \"ring\" / \"slime\" / \"nutrition\" / \"damage\"" },
  { name: "world.on_switch_toggled", category: "world", cancellable: false,
    whenFired: "a player flips a lever or switch",
    payload: [{ field: "player", type: "int" }, { field: "switch", type: "uid" }, { field: "state", type: "int" }],
    gotcha: "state is the NEW value after the flip (1 = on / powered, 0 = off)" },
  { name: "world.on_orb_placed", category: "world", cancellable: false,
    whenFired: "a player places an orb on a pedestal",
    payload: [{ field: "player", type: "int" }, { field: "pedestal", type: "uid" }, { field: "orb_type", type: "int" }, { field: "correct", type: "int" }],
    gotcha: "correct is 1 when the orb matched the pedestal (ritual advanced), 0 for the wrong orb" },
  { name: "world.on_teleport", category: "world", cancellable: false,
    whenFired: "a player uses a teleporter (pad or tunnel-spell)",
    payload: [{ field: "player", type: "int" }, { field: "teleporter", type: "uid" }, { field: "type", type: "int" }, { field: "dest_x", type: "int" }, { field: "dest_y", type: "int" }],
    gotcha: "same-floor teleport only; dest_x/dest_y are the destination tile. For descending floors use player.on_floor_change" },

  // ---- Custom events (sam_fire_hook) ------------------------------------------
  { name: "<namespace>:<hook_name>", category: "custom", cancellable: false,
    whenFired: "a script calls sam_fire_hook(\"namespace:name\", event); delivered cross-runtime (Lua<->JS<->TS) to every loaded script's on_event",
    payload: [{ field: "name", type: "string" }, { field: "<any user field>", type: "any" }],
    gotcha: "not a fixed event — the name is author-defined and MUST contain a colon; only number/bool/string fields survive the crossing; host-only; recursion capped at depth 8" },
];
